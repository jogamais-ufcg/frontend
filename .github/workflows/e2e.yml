name: e2e

on: deployment
jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Run tests on preview deployment
        uses: jogamais-ufcg/vercel-cypress-action@v1
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          repo: my-repo
